apiVersion: v1
kind: Service
metadata:
  name: sudo-system-api-nodeport
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: sudo-system-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
